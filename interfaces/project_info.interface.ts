export interface ProjectInfo {
  name: string;
  skills: string[];
  publishUrl?: string;
  reviewUrl?: string;
  notedUrl?: string;
  githubUrl?: string;
  videoUrl?: string;
  imageUrl: string;
  body: string
}
